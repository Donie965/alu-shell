#!/usr/bin/env bash
#!/bin/bash

# Find the PID of the 7-highlander process
pid=$(pgrep -f '[7]-highlander')

# Check if the PID was found
if [[ -n "$pid" ]]; then
    # Kill the process with SIGKILL (force termination)
    kill -9 "$pid"
    echo "OK"
fi
